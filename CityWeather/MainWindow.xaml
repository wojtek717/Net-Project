<Window x:Class="CityWeather.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CityWeather"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="425">

    <StackPanel Orientation="Vertical" Margin="10 10 10 10">
        <StackPanel Orientation="Horizontal">
            <TextBox Text="Wpisz miasto do obserwowania" Width="200" Padding="5" Margin="0 0 20 0"></TextBox>
            <Button Content="Dodaj miasto"></Button>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Label Content="Obserwowane miasta:"></Label>
        </StackPanel>
        
        <ListBox x:Name="citiesList"  Margin="0,0,0,0"  ItemsSource="{Binding}" Height="290">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Age}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Content="Pobierz nowe dane" Margin="0 15 0 0"></Button>

    </StackPanel>
</Window>
